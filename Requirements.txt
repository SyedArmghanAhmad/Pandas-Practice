Here’s the content for the `requirements.txt` file:
pandas==1.3.5
matplotlib==3.5.1
jupyter==1.0.0

Save this code in a file named `requirements.txt` in your project directory. You can later install these dependencies using:
pip install -r requirements.txt
